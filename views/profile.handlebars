<div class="container">
    <div class="row">
        <div class="col-sm-4">
            <img src="{{user.image}}" width="64px" height="64px">
            <p>{{user.firstname}}
            {{user.lastname}} </p>
            
        </div>
    </div>
</div>
